Install dependencies
```bash
apt install cargo sway waybar udiskie network-manager-applet python3-mako tofi foot pulseaudio-utils brightnessctl grim playerctl libnotify-bin wlogout autotiling 
cargo install sway-workspace-manager swaymsg_workspace swayfocus
```
